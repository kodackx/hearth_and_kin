services:
  hearthandkin:
    container_name: hearthandkin
    image: sjoeborg/hearthandkin:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - type: bind
        source: ./src
        target: /app/src
    ports:
      - 8000:8000
    env_file:
      - .env